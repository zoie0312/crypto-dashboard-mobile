import { Center } from "native-base";
import React from "react";

function TokenView({ navigation, route }) {
    return (
        <Center bg="primary.400" p="20">
            Token View
        </Center>
    );
}

export default TokenView;
